# Dependencies
node_modules
.pnp
.pnp.js

# Build & cache
.next
out
build
dist
*.tsbuildinfo

# Env and secrets (never commit real credentials)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env*.local

# Debug & logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Claude / local tooling
.claude/

# Cursor & other AI assistants (not part of the app)
.cursor/
.cursorrules
.cursorignore
AGENTS.md
.aider*
.continue/
.windsurfrules
.copilot/
*.canvas.tsx
agent-transcripts/
**/agent-transcripts/

# AI / agent scratch & plans
*.plan.md
PLAN.md
scratch/
temp/
tmp/

# Local Windows / shell scripts (machine-specific)
*.bat
*.cmd
*.ps1
!scripts/**/*.ps1

# Database dumps & backups — NOT prisma/migrations/**/*.sql (those stay tracked)
/*.sql
backup*.sql
*.sql.gz
*.dump
*.bak

# IDE & OS
.idea
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Prisma
prisma/migrations/**/migration_lock.toml

# Uploads (optional: commit sample or use .env for path)
# public/uploads/*
!public/uploads/.gitkeep

# Vercel
.vercel

# Testing
coverage
.nyc_output
